# AGENTS.md

このリポジトリで作業するエージェント向けのガイドです。リポジトリ全体に適用します。

## プロジェクト概要

- `bdspackman` は Minecraft Bedrock Dedicated Server のビヘイビアパック / リソースパックを管理する Go 製 TUI ツールです。
- エントリポイントは `cmd/bdspackman/main.go` です。
- アプリケーションの主要ロジックは `internal/` にあります。
- Bubble Tea / Lip Gloss を使った TUI 実装は `internal/tui/` にあります。
- CLI フラグ、設定補完、パックのスキャン、保存、移動、削除、リネームなどの処理は `internal/` 配下の用途別ファイルに分かれています。

## 開発環境

- Go のバージョンは `go.mod` に従ってください。
- 依存関係の取得は `make setup` または `go mod download` を使ってください。
- 通常ビルドは `make build` を使ってください。
- リリース用クロスビルドは `make release` を使ってください。
- 生成物は `build/` 配下に出力されます。

## コーディング規約

- Go コードは必ず `gofmt` で整形してください。
- パッケージ構成は既存の責務分離に合わせてください。
  - CLI 起動処理: `cmd/bdspackman/`
  - コアロジック: `internal/`
  - TUI 表示・入力・状態遷移: `internal/tui/`
- 既存の公開型やコメントのスタイルに合わせ、必要な exported identifier には簡潔なコメントを付けてください。
- エラーは呼び出し元に返し、CLI 層で標準エラーへ表示して終了する既存方針に合わせてください。
- ファイルシステム操作は `internal/fileops.go` など既存のヘルパーやパターンを優先して使ってください。
- TUI のキー操作や画面状態を変更する場合は、`internal/tui/actions.go`、`internal/tui/update.go`、`internal/tui/view.go`、`internal/tui/status.go` の責務分担を保ってください。
- ユーザー向け文言を追加・変更する場合は、既存の日本語 UI / README のトーンに合わせてください。

## テストと検証

- 変更後は少なくとも以下を実行してください。
  - `gofmt -w <変更した .go ファイル>`
  - `go test ./...`
- ビルドに関わる変更では `make build` も実行してください。
- パックの移動・削除・リネーム・JSON 出力などファイルシステムを変更する処理では、既存テストを参考に一時ディレクトリを使ったテストを追加・更新してください。
- TUI の状態遷移やステータスメッセージを変更する場合は、`internal/tui/` 配下のテストを追加・更新してください。

## ドキュメント

- CLI フラグ、ユーザーに見える機能、動作確認済み環境、実装状況が変わる場合は `README.md` を更新してください。
- README は現在日本語で書かれているため、追記も原則日本語にしてください。

## Git / PR

- 作業前後に `git status --short` を確認し、意図しない変更を含めないでください。
- コミットメッセージは変更内容が分かる簡潔なものにしてください。
- PR には概要と実行したテストを記載してください。
